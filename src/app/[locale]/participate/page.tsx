import type { Metadata } from "next";
import { notFound } from "next/navigation";
import { DemoForm } from "@/components/forms/DemoForm";
import { isLocale } from "@/i18n/config";
export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }): Promise<Metadata> { const { locale } = await params; return { title: locale === "ar" ? "طلب المشاركة" : "Participation request" }; }
export default async function ParticipatePage({ params }: { params: Promise<{ locale: string }> }) { const { locale: raw } = await params; if (!isLocale(raw)) notFound(); return <main><section className="page-hero"><div className="container"><h1>{raw === "ar" ? "طلب المشاركة" : "Participation request"}</h1><p>{raw === "ar" ? "أرسل طلبًا أوليًا، ثم يستكمل فريق الجمعية التواصل عبر البريد أو WhatsApp." : "Send an initial request and the association will follow up by email or WhatsApp."}</p></div></section><section className="section"><div className="container form-layout"><DemoForm locale={raw} type="participation" /><aside className="sidebar-card card"><h2>{raw === "ar" ? "ما الذي يحدث بعد الإرسال؟" : "What happens next?"}</h2><ol><li>{raw === "ar" ? "يظهر الطلب داخل لوحة الإدارة." : "The request appears in the admin inbox."}</li><li>{raw === "ar" ? "تراجع الإدارة البيانات." : "The team reviews the information."}</li><li>{raw === "ar" ? "يتم التواصل خارج الموقع." : "Follow-up happens outside the website."}</li></ol></aside></div></section></main>; }
