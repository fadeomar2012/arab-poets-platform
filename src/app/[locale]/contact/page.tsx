import type { Metadata } from "next";
import { notFound } from "next/navigation";
import { DemoForm } from "@/components/forms/DemoForm";
import { isLocale } from "@/i18n/config";
export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }): Promise<Metadata> { const { locale } = await params; return { title: locale === "ar" ? "تواصل معنا" : "Contact" }; }
export default async function ContactPage({ params }: { params: Promise<{ locale: string }> }) { const { locale: raw } = await params; if (!isLocale(raw)) notFound(); return <main><section className="page-hero"><div className="container"><h1>{raw === "ar" ? "تواصل معنا" : "Contact us"}</h1><p>{raw === "ar" ? "البريد الرسمي وWhatsApp هما قناتا التواصل الأساسيتان." : "Official email and WhatsApp are the primary contact channels."}</p></div></section><section className="section"><div className="container form-layout"><DemoForm locale={raw} type="contact" /><aside className="contact-stack"><div className="sidebar-card card"><h3>{raw === "ar" ? "البريد الرسمي" : "Official email"}</h3><a className="text-link" href="mailto:info@example.org">info@example.org</a></div><div className="sidebar-card card"><h3>WhatsApp</h3><p>{raw === "ar" ? "يضاف الرقم الرسمي لاحقًا." : "The official number will be added later."}</p></div><div className="sidebar-card card"><h3>Facebook</h3><a className="text-link" href="https://www.facebook.com/internationalarabpoets">internationalarabpoets</a></div></aside></div></section></main>; }
