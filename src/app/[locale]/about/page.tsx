import type { Metadata } from "next";
import Image from "next/image";
import { notFound } from "next/navigation";
import { PersonCard } from "@/components/people/PersonCard";
import { isLocale } from "@/i18n/config";
import { getPeople } from "@/lib/content";
export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }): Promise<Metadata> { const { locale } = await params; return { title: locale === "ar" ? "من نحن" : "About" }; }
export default async function AboutPage({ params }: { params: Promise<{ locale: string }> }) { const { locale: raw } = await params; if (!isLocale(raw)) notFound(); const people = await getPeople(); return <main><section className="page-hero"><div className="container"><h1>{raw === "ar" ? "من نحن" : "About us"}</h1><p>{raw === "ar" ? "مؤسسة ثقافية تعمل على جمع الشعراء وتوثيق الحراك الأدبي في دول عربية وأوروبية." : "A cultural institution connecting poets and documenting literary activity across Arab and European countries."}</p></div></section><section className="section"><div className="container about-grid"><div><span className="pill">{raw === "ar" ? "الجمعية" : "The association"}</span><h2>{raw === "ar" ? "نعيش لفكرة حرة، ونبني جسورًا للقصيدة العربية" : "We live for a free idea and build bridges for Arabic poetry"}</h2><p className="prose">{raw === "ar" ? "تنظم الجمعية المهرجانات والملتقيات والأمسيات والبرامج الثقافية، وتربط الشعراء بالجمهور والمؤسسات الثقافية، وتوثّق التجارب الأدبية للأجيال الحالية والقادمة." : "The association organizes festivals, forums, poetry readings, and cultural programs, connecting poets with audiences and cultural institutions while preserving literary work for future generations."}</p></div><div className="about-image"><Image src="/images/association-photo.png" alt="" fill sizes="(max-width: 760px) 100vw, 50vw" /></div></div></section><section className="section section-soft"><div className="container values-grid"><article className="card value-card"><span>◈</span><h3>{raw === "ar" ? "الرؤية" : "Vision"}</h3><p>{raw === "ar" ? "أن تكون الجمعية منصة دولية موثوقة للشعر العربي والتبادل الثقافي." : "To become a trusted international platform for Arabic poetry and cultural exchange."}</p></article><article className="card value-card"><span>✒</span><h3>{raw === "ar" ? "الرسالة" : "Mission"}</h3><p>{raw === "ar" ? "جمع الشعراء وتوثيق الفعاليات وإتاحة فرص جديدة للمشاركة والانتشار." : "To connect poets, document events, and create opportunities for participation and visibility."}</p></article><article className="card value-card"><span>◎</span><h3>{raw === "ar" ? "القيم" : "Values"}</h3><p>{raw === "ar" ? "الحرية والأصالة والتنوع والاحترام والتواصل بين الثقافات." : "Freedom, authenticity, diversity, respect, and intercultural dialogue."}</p></article></div></section><section className="section"><div className="container"><h2>{raw === "ar" ? "إدارة الجمعية" : "Association team"}</h2><div className="people-grid">{people.slice(0,4).map((person) => <PersonCard person={person} locale={raw} key={person.slug} />)}</div></div></section></main>; }
